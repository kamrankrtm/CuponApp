<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!--
          دریافت لحظه‌ای پیامک. اندروید این گیرنده را حتی وقتی اپ بسته است
          بیدار می‌کند، چون SMS_RECEIVED از محدودیت پخش‌های ضمنی مستثناست.
        -->
        <receiver
            android:name=".SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- کپی کد از روی خود اعلان، بدون باز کردن اپ -->
        <receiver
            android:name=".CopyCodeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.cuponapp.smsdiscounts.COPY_CODE" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <!-- برای تماس با سرویس هوش مصنوعی -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- خواندن پیامک‌های صندوق ورودی؛ هسته‌ی کار اپ -->
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- دریافت لحظه‌ای پیامک تازه، حتی وقتی اپ بسته است -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- اعلان کد تخفیف تازه؛ در اندروید ۱۳ به بعد مجوز زمان اجرا دارد -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      اختیاری: فقط برای نمایش نام اپراتور هر سیم‌کارت استفاده می‌شود.
      نبودِ این مجوز اپ را از کار نمی‌اندازد.
    -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <uses-feature android:name="android.hardware.telephony" android:required="false" />
</manifest>
