# راهنمای جامع و مرجع شناختی پروژه اپلیکیشن پیام‌رسان هوشمند کوپن‌اپ (CuponApp Native Android)

این سند مرجع اصلی و حافظه دائمی هوش مصنوعی برای درک معماری، ساختار ماژول‌ها و منطق کاری نسخه جدید **کوپن‌اپ (CuponApp)** بر پایه هسته قدرتمند **QKSMS** است.

---

## ۱. فلسفه و هدف پروژه (Philosophy & Purpose)
- **مسئله اصلی:** کاربران ایرانی روزانه ده‌ها پیامک تبلیغاتی، بانکی، کد ورود (OTP) و شخصی دریافت می‌کنند. پیامک‌های تبلیغاتی مکرر آزاردهنده‌اند، کدهای تخفیف باارزش گم می‌شوند و کپی کردن کدهای ورود نیاز به جابجایی دستی بین برنامه‌ها دارد.
- **راهکار پروژه:** یک اپلیکیشن پیام‌رسان بومی کامل اندروید (Default SMS App) که مستقیماً به پیامک‌های گوشی دسترسی دارد و آن‌ها را در ۵ زبانه هوشمند تفکیک می‌کند:
  1. **پیام‌های شخصی (Personal):** صرفاً شماره‌های `09...` یا مخاطبین ذخیره‌شده.
  2. **کدهای تخفیف (Discounts):** استخراج خودکار کدها، نام برند، مبلغ تخفیف و مهلت با امکان کپی ۱-کلیکه.
  3. **کدهای ورود (OTP):** استخراج هوشمند عدد ۴ تا ۸ رقمی، **کپی خودکار به کلیپ‌بورد گوشی** و اعلان درشت با دکمه کپی مجدد.
  4. **بانکی (Banking):** تراکنش‌های مالی، واریز و برداشت‌ها.
  5. **تبلیغات و اسپم (Spam):** پیامک‌های تبلیغاتی بدون تخفیف که بی‌صدا می‌شوند تا مزاحمت ایجاد نکنند.

---

## ۲. پشته فنی و ساختار پروژه (Technology Stack & Modules)
- **پلتفرم:** ۱۰۰٪ بومی اندروید (Native Android) با زبان **Kotlin**
- **ماژول‌ها:**
  - `presentation`: لایه رابط کاربری، فعالیت اصلی `MainActivity`، زبانه‌های بالا با `TabLayout`، آداپتورهای `PromoCodesAdapter`، `OtpCodesAdapter` و `FilteredConversationsAdapter`.
  - `domain`: منطق تجاری، مدیریت تنظیمات `Preferences`، موارد استفاده اینتراکتور `ReceiveSms`.
  - `data`: لایه ذخیره‌سازی، پایگاه داده Realm، رسیورهای پیامک `SmsReceiver`.
  - `common`: ابزارهای مشترک، تم‌ها، تاریخ، و تبدیل‌ها.
  - `android-smsmms`: درایور بومی ارتباط با سرویس پیامک و چندرسانه‌ای اندروید.
  - `web-archive`: آرشیو نسخه وب و PWA قبلی پروژه.
- **سیستم بیلد و CI/CD:**
  - گریدل با تسک اختصاصی `assembleNoAnalyticsDebug`
  - پایپ‌لاین خودکار GitHub Actions در مسیر `.github/workflows/build-apk.yml` که با هر Push فایل نصبی `CuponApp-debug.apk` را می‌سازد.

---

## ۳. کامپوننت‌های کلیدی هوشمندسازی (feature/smart)
1. **`SmartSmsClassifier.kt`**:
   - تفکیک بلادرنگ پیامک‌ها به ۵ دسته: شخصی، تخفیف، کد تایید (OTP)، بانکی و اسپم.
   - تشخیص برندهای ایرانی: اسنپ‌فود، تپسی‌فود، دیجی‌کالا، فیلیمو، سینماتیکت، اکالا، باسلام و...
2. **`SmartDataManager.kt`**:
   - اسکن و کش کردن کدهای تخفیف و کدهای ورود در حافظه و دسترسی سریع آداپتورها.
3. **`ClipboardHelper.kt`**:
   - تزریق آنی کد تایید به `ClipboardManager` گوشی و نمایش توست فارسی.
4. **`CopyClipReceiver.kt`**:
   - BroadcastReceiver اختصاصی برای اجرای اکشن «کپی کد» از داخل نوتیفیکیشن اندروید.

---

## ۴. مدیریت اعلان‌ها (NotificationManagerImpl.kt)
- **کانال کدهای تخفیف (`notifications_discounts`):** اعلان با تایتل درشت مبلغ تخفیف و نام برند، همراه با دکمه اختصاصی «کپی کد تخفیف».
- **کانال کدهای ورود (`notifications_otp`):** اعلان با اولویت بالا (Heads-up) همراه با دکمه «کپی مجدد کد».
- **کانال اسپم (`notifications_spam`):** اولویت مینیمم، بدون صدا و ویبره (و قابلیت خاموش‌سازی کامل در تنظیمات).

---

## ۵. تنظیمات شخصی‌سازی (SettingsController)
کاربر در بخش Settings می‌تواند رفتار زیر را کنترل کند:
- `prefAutoCopyOtp`: روشن/خاموش کردن کپی خودکار کد تایید به کلیپ‌بورد.
- `prefSilentSpam`: روشن/خاموش کردن بی‌صدا بودن پیام‌های تبلیغاتی اسپم.
- `prefNotifyDiscounts`: فعال/غیرفعال کردن اعلان شکیل کدهای تخفیف.
- `prefJalaliCalendar`: فعال/غیرفعال‌سازی تاریخ‌های هجری شمسی و ارقام فارسی.
- `prefMediaAsCloudLink`: جایگزینی خودکار MMS با آپلود ابری و ارسال لینک در پیامک.
- `prefFilesIrToken`: کلید اتصال API سرور Files.ir جهت آپلود عکس و ویدیو.
- `prefFilesIrEndpoint`: دامنه سرور Files.ir (پیش‌فرض: `https://my.files.ir`).
- `prefZayaApiKey`: کلید API کوتاه‌کننده زایا (zaya.io).
- `prefTestCloudConnection`: تست زنده وضعیت اتصال به سرویس ابری و کوتاه‌کننده لینک.

---

## ۶. قابلیت‌های ارتباطی و بومی‌سازی (Features 9-12)
1. **تقویم خورشیدی و ارقام فارسی (`JalaliCalendar.kt` & `DateFormatter.kt`):**
   - الگوریتم دقیق ۳۳ ساله تبدیل میلادی به شمسی بدون وابستگی جانبی.
   - نمایش تمام پیامک‌ها، لیست گفتگوها، کدهای ورود و تخفیف با تاریخ شمسی و ارقام فارسی.
2. **ارسال مستقیم در واتس‌اپ (`ic_whatsapp.xml`, `ComposeActivity.kt`, `QkReplyActivity.kt`):**
   - دکمه شکیل واتس‌اپ کنار دکمه ارسال پیامک.
   - باز شدن مستقیم صفحه چت واتس‌اپ با شماره استاندارد (`989...`) و متن آماده.
3. **موتور ابری جایگزین MMS ناموفق (`CloudUploadManager.kt`):**
   - آپلود خودکار عکس و ویدیو به پلتفرم Files.ir (`https://my.files.ir/api/v1/uploads`) با قابلیت فال‌بک رایگان.
   - کوتاه‌سازی خودکار لینک با API اختصاصی زایا (`zaya.io/api/v1/links`).
   - درج خودکار لینک در متن پیامک (مثال: `📷 عکس: https://zaya.io/...`).
4. **رابط کاربری مینیمال پروگرس آپلود (`cloud_upload_dialog.xml`):**
   - دیالوگ مدرن لودینگ Material با نمایش درصد پیشرفت و دکمه انصراف (Cancel).

