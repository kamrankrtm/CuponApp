# راهنمای جامع و مرجع شناختی پروژه اپلیکیشن پیام‌رسان هوشمند کوپن‌اپ (CuponApp Native Android)

این سند مرجع اصلی و حافظه دائمی هوش مصنوعی برای درک معماری، ساختار ماژول‌ها و منطق کاری نسخه جدید **کوپن‌اپ (CuponApp)** بر پایه هسته قدرتمند **QKSMS** است.

---

## ۱. فلسفه و هدف پروژه (Philosophy & Purpose)
- **مسئله اصلی:** کاربران ایرانی روزانه ده‌ها پیامک تبلیغاتی، بانکی، کد ورود (OTP) و شخصی دریافت می‌کنند. پیامک‌های تبلیغاتی مکرر آزاردهنده‌اند، کدهای تخفیف باارزش گم می‌شوند و کپی کردن کدهای ورود نیاز به جابجایی دستی بین برنامه‌ها دارد.
- **راهکار پروژه:** یک اپلیکیشن پیام‌رسان بومی کامل اندروید (Default SMS App) که مستقیماً به پیامک‌های گوشی دسترسی دارد و آن‌ها را در ۵ زبانه هوشمند تفکیک می‌کند:
  1. **پیام‌های شخصی (Personal):** صرفاً شماره‌های `09...` یا مخاطبین ذخیره‌شده.
  2. **کدهای تخفیف (Discounts):** استخراج خودکار کدها، نام برند، مبلغ تخفیف و مهلت با امکان کپی ۱-کلیکه.
  3. **کدهای ورود (OTP):** استخراج هوشمند عدد ۴ تا ۸ رقمی، **کپی خودکار به کلیپ‌بورد گوشی** و اعلان درشت با دکمه کپی مجدد.
  4. **بانکی (Banking):** تراکنش‌های مالی، واریز و برداشت‌ها.
  5. **تبلیغات و اسپم (Spam):** پیامک‌های تبلیغاتی بدون تخفیف که بی‌صدا می‌شوند تا مزاحمت ایجاد نکنند.

---

## ۲. پشته فنی و ساختار پروژه (Technology Stack & Modules)
- **پلتفرم:** ۱۰۰٪ بومی اندروید (Native Android) با زبان **Kotlin**
- **ماژول‌ها:**
  - `presentation`: لایه رابط کاربری، فعالیت اصلی `MainActivity`، زبانه‌های بالا با `TabLayout`، آداپتورهای `PromoCodesAdapter`، `OtpCodesAdapter` و `FilteredConversationsAdapter`.
  - `domain`: منطق تجاری، مدیریت تنظیمات `Preferences`، موارد استفاده اینتراکتور `ReceiveSms`.
  - `data`: لایه ذخیره‌سازی، پایگاه داده Realm، رسیورهای پیامک `SmsReceiver`.
  - `common`: ابزارهای مشترک، تم‌ها، تاریخ، و تبدیل‌ها.
  - `android-smsmms`: درایور بومی ارتباط با سرویس پیامک و چندرسانه‌ای اندروید.
  - `web-archive`: آرشیو نسخه وب و PWA قبلی پروژه.
- **سیستم بیلد و CI/CD:**
  - گریدل با تسک اختصاصی `assembleNoAnalyticsDebug`
  - پایپ‌لاین خودکار GitHub Actions در مسیر `.github/workflows/build-apk.yml` که با هر Push فایل نصبی `CuponApp-debug.apk` را می‌سازد.

---

## ۳. کامپوننت‌های کلیدی هوشمندسازی (feature/smart)
1. **`SmartSmsClassifier.kt`**:
   - تفکیک بلادرنگ پیامک‌ها به ۵ دسته: شخصی، تخفیف، کد تایید (OTP)، بانکی و اسپم.
   - برای تخفیف‌ها صرفاً هماهنگ‌کننده است؛ تشخیص برند و استخراج مقادیر در `feature/smart/promo` انجام می‌شود.
2. **`SmartDataManager.kt`**:
   - کش ترد-سیف کدهای تخفیف و کدهای ورود. کدهای تخفیف از طریق `PromoStore` روی دیسک ماندگار می‌شوند
     (وضعیت «استفاده شد»، «کار نمی‌کند» و «نشان‌شده» پس از بستن اپ باقی می‌ماند)؛ کدهای OTP عمداً
     فقط در حافظه می‌مانند.
   - با `SmartDataManager.init(context)` در `QKApplication` مقداردهی می‌شود، نه در `MainActivity`،
     چون پیامک رسیده در پس‌زمینه ممکن است بدون هیچ Activity پردازش شود.
3. **`ClipboardHelper.kt`**:
   - تزریق آنی کد تایید به `ClipboardManager` گوشی و نمایش توست فارسی.
4. **`CopyClipReceiver.kt`**:
   - BroadcastReceiver اختصاصی برای اجرای اکشن «کپی کد» از داخل نوتیفیکیشن اندروید.

### ۳.۱ موتور کدهای تخفیف (`feature/smart/promo`)
این بسته عمداً **بدون هیچ وابستگی به اندروید** نوشته شده تا با تست واحد روی JVM قابل راستی‌آزمایی باشد
(`presentation/src/test/.../PromoExtractionTest.kt`، `PromoIntelligenceTest.kt` و `AiPrivacyFilterTest.kt`).

- **`BrandRegistry.kt`** — تنها منبع حقیقت برای برندهای ایرانی: نام فارسی/انگلیسی، کلیدواژه‌ها،
  سرشماره‌ها، دسته، رنگ برند، پکیج اپ و دامنه. هر برند یک **نقش** دارد (`MERCHANT` فروشگاه،
  `PAYMENT` کیف پول/اقساطی، `BANK`، `TELECOM`) و می‌تواند عضو یک **خانواده** باشد
  (`snapp`، `tapsi`، `digikala`) با **کلمات زمینه** (`cues`): «اسنپ» + «غذا» یعنی اسنپ‌فود،
  «اسنپ» + «هتل» یعنی اسنپ‌تریپ، «تپسی» + «موتوپیک» یعنی پیک تپسی. تکه‌ای از خود کد (`codeHints`،
  مثل `BOX` در `TPSBOXH24`) هم سرنخ حساب می‌شود؛ اگر هیچ سرنخی نباشد، کارت به‌جای حدسِ «تاکسی
  اینترنتی» با `familyLabel` («سرویس‌های اسنپ») نشان داده می‌شود. تطبیق **کلمه‌ی کامل** و **طولانی‌ترین-مچ** است («ترب» در «تربیت» مچ
  نمی‌شود، «تپسی فود» هرگز به «تپسی» فرو نمی‌ریزد). نام‌هایی که واژه‌ی روزمره‌اند («دیوار»، «باسلام»)
  در `weakKeywords` می‌روند و فقط با زمینه («در دیوار»، «اپلیکیشن طاقچه») یا فرستنده‌ی همان برند
  شمرده می‌شوند. برای افزودن برند جدید فقط یک ردیف به `BRANDS` اضافه کنید.
- **`BrandResolver.kt`** — «کد کجا استفاده می‌شود و از طرف کیست». فروشگاهِ نزدیک کد = مقصد؛
  کیف پول/بانک/اپراتور = **حامی** (`issuer`) و در صورت اشاره، **روش پرداخت** (`payWith`).
  کد دیجی‌پی برای فیلیمو ← برند «فیلیمو» با زیرنویس «از طرف دیجی‌پی · پرداخت با دیجی‌پی»؛
  کدی که برای چند فروشگاه است ← برند حامی با فهرست `usableAt`. نام خانواده‌ی خالی («اسنپ») با
  فرستنده یا کلمات زمینه به سرویس دقیق تبدیل می‌شود و در غیر این صورت اطمینان پایین می‌گیرد.
- **`PromoValueParser.kt`** — تبدیل متن آزاد فارسی به عدد: مبلغ، درصد، **سقف تخفیف**، حداقل سبد و
  تاریخ انقضا. مبلغ نزدیک به واژه‌ی «تخفیف» و نزدیک کد انتخاب می‌شود (نه اولین عدد؛ «قیمت ۱۲ میلیون»
  تخفیف نیست). ریال را به تومان تبدیل می‌کند، اعداد با حروف («صد و پنجاه هزار»، «یک و نیم میلیون»)،
  «۵۰ت» و حداقل خرید بدون واحد را می‌خواند. مهلت‌ها: تاریخ، نام ماه، «تا پایان مهر/ماه/هفته»، روز هفته،
  «امشب»، «تا فردا»، و بازه فقط کنار واژه‌ای مثل «تا/مهلت/اعتبار» («ارسال ۲ ساعته» مهلت نیست؛ «از فردا» هم).
  `normalize` کشیده، نیم‌فاصله، نویسه‌های نامرئی و جداکننده‌های عربی را یکدست می‌کند.
  `DEFAULT_VALIDITY_DAYS` تنها مرجع مهلت پیش‌فرض است.
- **`PromoCodeExtractor.kt`** — استخراج **همه‌ی** کدهای پیام به همراه **امتیاز اطمینان** و مکان؛ برچسب
  ممکن است با چند واژه‌ی پرکننده یا یک خط فاصله از کد جدا باشد. «کد رهگیری/پیگیری/سفارش…» کد تخفیف
  نیست، توکن داخل لینک کد نیست، و پیام «کد اعمال شد/منقضی شد» پیشنهاد نیست. کد نامطمئن حذف نمی‌شود
  بلکه در کارت برچسب می‌خورد، در رتبه‌بندی پایین می‌آید و نامزد لایه‌ی هوش مصنوعی می‌شود.
- **`PromoParser.kt`** — هماهنگ‌کننده: کدها + برند + مقدارها را به کارت تبدیل می‌کند (برای پیام چندکدی،
  هر کد مقدار و برند بخش خودش را می‌گیرد) و برای هر کارت **اطمینان کد** و **اطمینان برند** جدا گزارش
  می‌دهد. ترتیب اعتماد: پاسخ AI ذخیره‌شده برای همین پیام ← موتور قواعد (با برندِ آموخته‌شده از قالب یا
  فرستنده) ← نام امضاشده در ابتدای پیام («فروشگاه لباس رز: …»).
- **`PromoRanker.kt`** — رتبه‌بندی بر اساس فوریت انقضا و ارزش تخمینی (با احتساب سقف تخفیف)، و
  گروه‌بندی به بخش‌های «امروز/این هفته/بعدتر/بدون مهلت». همچنین منبع فهرست یادآوری‌های انقضا.
- **`PromoCodec.kt` / `PromoStore.kt`** — سریالایز JSON (نسخه ۳، نسخه ۲ هم خوانده می‌شود) و ذخیره روی
  `SharedPreferences`، به‌علاوه‌ی نشانگر اسکن افزایشی، اجازه‌ی AI، حافظه‌ی AI، شمارنده‌ی مصرف
  روزانه/ماهانه و `ENGINE_VERSION` (افزایشش یک بار صندوق را دوباره می‌خواند؛ علامت‌های کاربر با کلید
  «پیام + کد» حفظ می‌شوند حتی اگر نام برند عوض شود). کارت‌ها با خوانشِ نسخه‌ای که ساختشان ذخیره
  می‌شوند؛ برای همین پس از **هر به‌روزرسانی** اپ، `SmartDataManager.refileAll()` همه‌ی کارت‌های
  ذخیره‌شده را از متن پیامک خودشان دوباره می‌خواند (`needsRefile`)، نه فقط پیامک‌های اخیر صندوق.
- **`PromoExpiryNotifier.kt`** — هشدار پیش از سوختن کدهای باارزش، با دکمه‌ی کپی در نوتیفیکیشن.
- **`AiPrivacyFilter.kt`** — دروازه‌ی حریم خصوصی. پیامک بانکی، OTP، قضایی، سلامت، شماره‌های شخصی و
  هر متنی حاوی شماره کارت/شبا/کد ملی **هرگز** از دستگاه خارج نمی‌شود، حتی اگر واژگان تبلیغاتی داشته باشد.
  واژه‌های کوتاه («پایا»، «ثنا»، «وام») فقط به‌صورت کلمه‌ی کامل مچ می‌شوند تا «تا پایان هفته» مسدود نشود.
  «قسط» فقط در پیامی مجاز است که برچسب کوپن دارد («کد تخفیف قسطی»)؛ یادآوری قسط و واژه‌های بدهی
  («سررسید»، «معوق»، «بدهی»، «دیرکرد») همیشه مسدودند.
  هر تغییری در این فایل باید با تست همراه باشد؛ رگرسیون اینجا باگ قابل‌مشاهده تولید نمی‌کند، بلکه
  بی‌صدا داده‌ی واقعی کاربر را نشت می‌دهد.

### ۳.۲ لایه هوش مصنوعی (`feature/smart/ai` و `promo/Ai*`)
`AiPromoExtractor` یک **لایه‌ی دوم** است، نه مسیر اصلی، و هر مرحله‌اش برای کم‌هزینه بودن طراحی شده:
- **`AiEscalation.kt`** تصمیم می‌گیرد کدام پیامک ارزش ارسال دارد: فقط وقتی اطمینان کد یا برند زیر ۷۵
  است یا عددی در متن خوانده نشده، پیام اصلاً توکنی شبیه کد دارد (AI فقط کدی را می‌تواند بدهد که در متن
  است) و فیلتر حریم خصوصی اجازه می‌دهد. بقیه (اکثریت مطمئن، پیام بدون کد، حساس) هزینه‌ای ندارند.
- **`PromoMemory.kt`** هر پاسخ AI را نگه می‌دارد: به ازای **پیام** (دیگر هرگز ارسال نمی‌شود، حتی پس از
  اسکن مجدد)، به ازای **قالب کمپین** (پیام بعدی همان فرستنده با همان متن و کد متفاوت، برند و جای کد را
  بدون درخواست می‌گیرد) و به ازای **فرستنده** (فروشگاه کوچکی که دو بار یکسان شناخته شد). در هر اسکن،
  از هر کمپین فقط یک پیام ارسال و پاسخش روی بقیه اعمال می‌شود.
- **`AiPromoProtocol.kt`** درخواست را فشرده می‌سازد (دستور ثابت کوتاه، حذف لینک به‌جز دامنه، خط لغو،
  ایموجی و نام کاربر، سقف طول، کلیدهای یک‌حرفی در پاسخ، `max_tokens`) و پاسخ را با تحمل خطا می‌خواند.
- پاسخ AI مستقیماً کارت نمی‌شود: در حافظه ذخیره و پیام دوباره با `PromoParser` خوانده می‌شود؛ کدی که در متن
  نیست دور ریخته می‌شود و برندی که پیام پشتیبانی‌اش نمی‌کند پذیرفته نمی‌شود. پاسخ مبهم AI (فقط «اسنپ»
  یا «تپسی») هرگز جای خوانشی را که سرویس را پیدا کرده («اسنپ‌شاپ»، «تپسی موتوپیک») نمی‌گیرد
  (`moreSpecific`)؛ چنین پاسخ‌هایی قبلاً کش می‌شدند و کارت را روی «تاکسی اینترنتی» نگه می‌داشتند.
- حالت **«بررسی همه‌ی کدها»** (`aiCheckAll`، پیش‌فرض روشن): هر پیامک تبلیغاتی دارای کد یک بار به AI
  می‌رود، نه فقط موارد نامطمئن؛ حریم خصوصی، کش پاسخ‌ها و سقف روزانه (پیش‌فرض ۱۰۰) همچنان برقرارند.
- اجرای **خودکار** (پس از اسکن صندوق و دریافت پیامک، با تأخیر برای دسته‌کردن) فقط با کلید API،
  رضایت کاربر (`AiConsentDialog`) و تا **سقف روزانه** انجام می‌شود؛ اسکن دستی هم در دسترس است.
  مصرف روزانه/ماهانه (پیام و توکن) در تنظیمات نمایش داده می‌شود.

---

## ۴. مدیریت اعلان‌ها (NotificationManagerImpl.kt)
- **کانال کدهای تخفیف (`notifications_discounts`):** اعلان با تایتل درشت مبلغ تخفیف و نام برند، همراه با دکمه اختصاصی «کپی کد تخفیف».
- **کانال کدهای ورود (`notifications_otp`):** اعلان با اولویت بالا (Heads-up) همراه با دکمه «کپی مجدد کد».
- **کانال اسپم (`notifications_spam`):** اولویت مینیمم، بدون صدا و ویبره (و قابلیت خاموش‌سازی کامل در تنظیمات).

---

## ۵. تنظیمات شخصی‌سازی (SettingsController)
کاربر در بخش Settings می‌تواند رفتار زیر را کنترل کند:
- `prefAutoCopyOtp`: روشن/خاموش کردن کپی خودکار کد تایید به کلیپ‌بورد.
- `prefSilentSpam`: روشن/خاموش کردن بی‌صدا بودن پیام‌های تبلیغاتی اسپم.
- `prefNotifyDiscounts`: فعال/غیرفعال کردن اعلان شکیل کدهای تخفیف.
- `prefJalaliCalendar`: فعال/غیرفعال‌سازی تاریخ‌های هجری شمسی و ارقام فارسی.
- `prefMediaAsCloudLink`: جایگزینی خودکار MMS با آپلود ابری و ارسال لینک در پیامک.
- `prefFilesIrToken`: کلید اتصال API سرور Files.ir جهت آپلود عکس و ویدیو.
- `prefFilesIrEndpoint`: دامنه سرور Files.ir (پیش‌فرض: `https://my.files.ir`).
- `prefZayaApiKey`: کلید API کوتاه‌کننده زایا (zaya.io).
- `prefTestCloudConnection`: تست زنده وضعیت اتصال به سرویس ابری و کوتاه‌کننده لینک.
- `prefAiAutoRefine`: بررسی خودکار کدهای نامطمئن با هوش مصنوعی در پس‌زمینه.
- `prefAiCheckAll`: بررسی همه‌ی کدهای تخفیف با هوش مصنوعی (دقیق‌ترین) یا فقط موارد نامطمئن (کم‌هزینه‌ترین).
- `prefAiDailyLimit`: سقف تعداد پیامک ارسالی به هوش مصنوعی در روز، همراه با نمایش مصرف امروز و این ماه.

---

## ۶. قابلیت‌های ارتباطی و بومی‌سازی (Features 9-12)
1. **تقویم خورشیدی و ارقام فارسی (`JalaliCalendar.kt` & `DateFormatter.kt`):**
   - الگوریتم دقیق ۳۳ ساله تبدیل میلادی به شمسی بدون وابستگی جانبی.
   - نمایش تمام پیامک‌ها، لیست گفتگوها، کدهای ورود و تخفیف با تاریخ شمسی و ارقام فارسی.
2. **ارسال مستقیم در واتس‌اپ (`ic_whatsapp.xml`, `ComposeActivity.kt`, `QkReplyActivity.kt`):**
   - دکمه شکیل واتس‌اپ کنار دکمه ارسال پیامک.
   - باز شدن مستقیم صفحه چت واتس‌اپ با شماره استاندارد (`989...`) و متن آماده.
3. **موتور ابری جایگزین MMS ناموفق (`CloudUploadManager.kt`):**
   - آپلود خودکار عکس و ویدیو به پلتفرم Files.ir (`https://my.files.ir/api/v1/uploads`) با قابلیت فال‌بک رایگان.
   - کوتاه‌سازی خودکار لینک با API اختصاصی زایا (`zaya.io/api/v1/links`).
   - درج خودکار لینک در متن پیامک (مثال: `📷 عکس: https://zaya.io/...`).
4. **رابط کاربری مینیمال پروگرس آپلود (`cloud_upload_dialog.xml`):**
   - دیالوگ مدرن لودینگ Material با نمایش درصد پیشرفت و دکمه انصراف (Cancel).

