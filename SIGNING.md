# کلید امضای اپ

اندروید اجازه نمی‌دهد اپی که با یک کلید امضا شده، با APK ای که کلید دیگری
دارد جایگزین شود. بدون کلید ثابت، هر بیلد امضای متفاوتی می‌گیرد و نصب روی
نسخه قبلی با خطای «App not installed» شکست می‌خورد.

این کار فقط یک بار لازم است.

## ۱. ساخت کلید

روی کامپیوتر خودتان (`keytool` همراه جاوا نصب می‌شود). به‌جای
`<رمز-شما>` یک رمز قوی بگذارید:

```bash
keytool -genkeypair \
  -keystore cuponapp-release.keystore \
  -alias cuponapp \
  -keyalg RSA -keysize 4096 -validity 10950 \
  -storepass '<رمز-شما>' \
  -keypass  '<رمز-شما>' \
  -dname "CN=CuponApp, O=CuponApp, C=IR"
```

## ۲. جای فایل

فایل ساخته‌شده را اینجا بگذارید:

```
android/app/cuponapp-release.keystore
```

## ۳. رمز

یکی از این دو راه:

**الف) رمز در Secret مخزن — پیشنهادی**

در `Settings → Secrets and variables → Actions` این دو Secret را بسازید و
رمز خودتان را در هر دو بگذارید:

- `KEYSTORE_PASSWORD`
- `KEY_PASSWORD`

با این روش، حتی اگر مخزن لو برود کلید بدون رمز قابل استفاده نیست.

**ب) رمز کنار کد — ساده‌تر ولی ضعیف‌تر**

فایلی به نام `keystore.properties` در ریشه پروژه بسازید:

```properties
storePassword=<رمز-شما>
keyPassword=<رمز-شما>
keyAlias=cuponapp
```

این فایل در `.gitignore` هست. برای اینکه بیلد ابری هم از آن استفاده کند
باید عمداً کامیتش کنید — که یعنی کلید و رمز هر دو در مخزن‌اند و هر کسی که
به مخزن دسترسی پیدا کند می‌تواند APK جعلی بسازد که گوشی شما به‌عنوان
بروزرسانی قبولش کند.

> متغیر محیطی همیشه بر فایل اولویت دارد، پس می‌توانید محلی از فایل و در
> CI از Secret استفاده کنید.

## نکته‌های مهم

- **فایل `.keystore` را گم نکنید.** اگر گم شود، دیگر نمی‌توانید نسخه‌های
  بعدی را روی نصب فعلی بروزرسانی کنید.
- نسخه‌های پیش از این هرکدام با کلیدی تصادفی امضا شده‌اند، پس **یک بار
  دیگر** باید اپ را پاک و نصب کنید. از آن پس بروزرسانی‌ها بدون پاک کردن
  کار می‌کنند.
- اگر نه Secret و نه فایل رمز موجود نباشد، بیلد شکست نمی‌خورد؛ با کلید
  موقت امضا می‌شود و در لاگ هشدار می‌دهد.
