<?xml version="1.0" encoding="utf-8"?>
<!-- The app icon as drawn inside the app (drawer header): the gradient squircle and glyph.
     A shape gradient rather than a vector one, so it renders the same on API 21. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="12dp" />
            <gradient
                android:angle="270"
                android:endColor="#0A5CE0"
                android:startColor="#3B93FF" />
        </shape>
    </item>
    <item android:drawable="@drawable/app_icon_glyph" />
</layer-list>
